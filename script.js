// Automatically highlight dots on i and j
//document.addEventListener('DOMContentLoaded', () => {
//  const quoteElement = document.getElementById('quote');
//  const text = quoteElement.innerText;
//  const transformed = text.replace(/([ij])/g, '<span class="dot">$1</span>');
//  quoteElement.innerHTML = transformed;
//});